listeners:
  - name: PlayerOnline
    type: PlayerOnline
  - name: resources
    type: OSResources
    interval: 600 # Interval in ticks

storages:
  - name: WebSocket
    type: WebSocket
    port: 8080
  - name: FileConsumer
    type: Csv
    filepath: /data/stats.csv
    max_size: 100
    interval: 600 # Interval in ticks
  